from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Mean Opinion Score (MOS) is one of the common metrics to measure audio quality. In other words MOS is the average of call quality ratings given by different users when a VOIP call ended. ',
    author='Allen Ansari',
    license='',
)
